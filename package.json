{
  "name": "pieceForSmartphoneOrchestra",
  "version": "1.0.0",
  "description": "A music piece for smartphone orchestra.",
  "scripts": {
    "parcel-build": "parcel build src/views/index.html src/views/directions.html src/views/instrument.html --dist-dir build",
    "parcel-watch": "parcel watch src/views/index.html src/views/directions.html src/views/instrument.html --dist-dir dist",
    "prestart-dev": "if [[ ! -d dist ]]; then echo Directory 'dir' does not exist. Run: 'npm run parcel-watch'.; false; else true; fi",
    "start-dev": "if [[ $? == 0 ]]; then node server.js; fi",
    "prestart-prod": "if [[ ! -d build ]]; then echo Directory 'build' does not exist. Switch to branch master or run: 'npm run parcel-build'.; false; else true; fi",
    "start-prod": "if [[ $? == 0 ]]; then NODE_ENV=production node server.js; fi",
    "premkcert": "if [[ ! -d certs ]]; then mkdir ./certs; fi",
    "mkcert": "mkcert -key-file certs/smartphoneOrchestra-key.pem --cert-file certs/smartphoneOrchestra-crt.pem localhost 192.168.10.2"
  },
  "author": "Tassos Tsesmetzis",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "minimist": "^1.2.5"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.0-rc.0",
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "@parcel/transformer-webmanifest": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0",
    "parcel-config-precache-manifest": "^0.0.4",
    "standard": "^16.0.3"
  }
}
